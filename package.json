{
  "name": "synoem-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check:biome": "biome check --write .",
    "format:biome": "biome format --write .",
    "dmno": "pnpm exec dmno plugin",
    "vault": "pnpm exec dmno plugin -p vault/prod -- setup",
    "vault:add": "pnpm exec dmno plugin -p vault/prod -- add",
    "vault:delete": "pnpm exec dmno plugin -p vault/prod -- delete",
    "vault:upsert": "pnpm exec dmno plugin -p vault/prod -- upsert"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@synoem/typescript-config": "workspace:*",
    "turbo": "^2.5.3",
    "typescript": "catalog:",
    "vitest": "^3.1.4"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@dmno/cloudflare-platform": "^0.0.4",
    "@dmno/encrypted-vault-plugin": "^0.0.10",
    "@dmno/netlify-platform": "^0.0.5",
    "@dmno/vercel-platform": "^0.0.3",
    "dmno": "^0.0.39"
  },
  "pnpm": {
    "overrides": {
      "react-is": "catalog:",
      "file-type": "^20.5.0"
    }
  }
}
