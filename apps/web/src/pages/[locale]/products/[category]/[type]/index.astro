---
import ProductsLayout from "~/layouts/ProductsLayout.astro";
import type { Product } from "@synoem/payload/payload-types";
const url = new URL(Astro.request.url);
const initialParams = Object.fromEntries(url.searchParams);

const { category, type } = Astro.params;

const initialFilters = {
  ...initialParams,
};
---

<ProductsLayout
  title={`Just for test ${type}`}
  description={`${type} products`}
  category={category as Product["category"]}
  initialFilters={initialFilters}
/>
