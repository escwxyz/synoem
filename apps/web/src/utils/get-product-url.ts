// import type { Product } from "@synoem/payload/payload-types";
// import { isSolarPanel } from "./check-product-type";

// export const getProductUrl = (product: Product): string => {
//   if (isSolarPanel(product)) {
//     return `/products/solar-panels/${product.solarPanel?.[0].type}/${product.slug}`;
//   }

//   return `/products/pump-controllers/${product.pumpController?.[0].type}/${product.slug}`;
// };
